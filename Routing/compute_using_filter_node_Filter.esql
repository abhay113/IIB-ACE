
CREATE FILTER MODULE compute_using_filter_node_Filter
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN

		DECLARE inRef REFERENCE to Root.XMLNSC.EMP.COUNTRY;
		IF inRef = 'IND' THEN
			RETURN TRUE;
		ELSE
			RETURN FALSE;
		END IF;
	END;

END MODULE;